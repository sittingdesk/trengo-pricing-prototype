/**
 * Prototype iterations for the bottom-left version switcher. A meta/dev control
 * (not product UI) — add entries here as new iterations are built.
 */
export interface Iteration {
  id: number
  label: string
}

export const iterations: Iteration[] = [
  { id: 1, label: 'Iteration 1' },
  { id: 2, label: 'Iteration 2' },
]
